# 确定的概率（Deterministic Chance）

把 Minecraft 模组机器的概率产出变成可预测、可持久化的确定序列，并为 AE2 生成精确的批量处理样板。

例如配方 `1 A → 80% 1 B`：

- 机器执行序列固定为 `出、出、出、出、不出`，每 5 次循环；
- AE2 处理样板编码为 `5 A → 4 B`；
- 主产物、副产物和多个概率产物使用同一个批次的精确整数结果。

## 当前状态

仓库目前包含第一阶段的通用核心：

- 概率分数化与约分；
- 可持久化的确定序列；
- 多输入、多输出、副产物的最小精确批次计算；
- 机器与配方适配 API；
- 单元测试。

下一阶段将加入 AE2 样板编码界面和首批机器适配。目标环境为 Minecraft 1.20.1、Forge 47.4.3。

## 为什么仍需要机器适配

Forge、AE2 和 JEI 都没有统一的“概率输出”协议。JEI 负责展示配方，不负责定义机器实际如何掷概率；每个机器模组也可能把概率写在不同的配方类或执行流程里。

因此本项目采用“通用核心 + 模组级适配器”：同一模组若共享一个中央产出实现，通常只需要一个适配器即可覆盖其多数机器，不需要逐台机器修改。详见 [架构说明](docs/ARCHITECTURE.md)。

## 构建

```bash
./gradlew build
```

## License

MIT
